#!/bin/bash


echo "Will remove the following:"
echo
RES="$(docker ps -aqf status=exited)"
echo "${RES}"

read -p "Are you sure? " -n 1 -r
echo    # (optional) move to a new line
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
  exit 1
fi

echo "${RES}" | xargs docker rm
