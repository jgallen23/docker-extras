#!/bin/bash


echo "Will remove the following:"
echo
docker ps -aqf status=exited  | grep -v "$1"

read -p "Are you sure? " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
  exit 1
fi

docker rm $(docker ps -aqf status=exited | grep -v "$1" | awk '{ print $1 }')


